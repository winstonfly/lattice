apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: agentidentities.alattice.io
spec:
  group: alattice.io
  names:
    kind: AgentIdentity
    listKind: AgentIdentityList
    plural: agentidentities
    shortNames:
    - agentid
    singular: agentidentity
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .spec.peerRef
      name: PEER
      type: string
    - jsonPath: .spec.sandbox
      name: SANDBOX
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AgentIdentity binds an AI Agent's WireGuard Peer to its control-plane
          RBAC permissions.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentIdentitySpec defines the desired state of an AgentIdentity.
            properties:
              allowedNamespaces:
                description: |-
                  AllowedNamespaces restricts which K8s namespaces this agent's tool calls may affect.
                  Empty list means no namespaces are permitted.
                items:
                  type: string
                type: array
              allowedTools:
                description: |-
                  AllowedTools is the whitelist of tool names this agent may call.
                  Empty list means no tools are permitted.
                items:
                  type: string
                type: array
              auditLevel:
                default: write
                description: AuditLevel controls tool call logging verbosity for this
                  agent.
                type: string
              enforcementMode:
                default: enforce
                description: EnforcementMode can override the global agent-isolation
                  enforcement mode for this agent.
                type: string
              expiresAt:
                description: |-
                  ExpiresAt is the optional expiry time for this agent identity.
                  After this time the controller transitions the phase to Expired.
                  Nil means the identity never expires.
                format: date-time
                type: string
              peerRef:
                description: PeerRef is the name of the LatticePeer this agent is
                  bound to. Required.
                type: string
              sandbox:
                default: none
                description: Sandbox controls the hosting isolation mode for this
                  agent.
                type: string
            required:
            - peerRef
            type: object
          status:
            description: AgentIdentityStatus defines the observed state of an AgentIdentity.
            properties:
              conditions:
                description: Conditions reflect control-plane sync state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSeenAt:
                description: LastSeenAt is the last time the agent made an authenticated
                  API call.
                format: date-time
                type: string
              peerIP:
                description: PeerIP is the VPN IP address allocated to this agent's
                  LatticePeer.
                type: string
              phase:
                description: Phase is the current lifecycle phase.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: latticeclusterpeerings.alattice.io
spec:
  group: alattice.io
  names:
    kind: LatticeClusterPeering
    listKind: LatticeClusterPeeringList
    plural: latticeclusterpeerings
    shortNames:
    - wfcpeering
    singular: latticeclusterpeering
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .status.localCIDR
      name: LOCAL-CIDR
      type: string
    - jsonPath: .status.remoteCIDR
      name: REMOTE-CIDR
      type: string
    - jsonPath: .spec.remoteCluster
      name: REMOTE-CLUSTER
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          LatticeClusterPeering connects a local workspace network with a network in a
          remote Lattice cluster via the gateway-mode peering mechanism.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LatticeClusterPeeringSpec declares a cross-cluster network
              peering.
            properties:
              localNamespace:
                description: LocalNamespace is the workspace namespace in this cluster.
                type: string
              localNetwork:
                description: LocalNetwork is the LatticeNetwork name in LocalNamespace.
                type: string
              remoteCluster:
                description: |-
                  RemoteCluster references a LatticeCluster resource that describes the
                  remote Lattice deployment.
                type: string
              remoteNamespace:
                description: RemoteNamespace is the workspace namespace in the remote
                  cluster.
                type: string
              remoteNetwork:
                description: RemoteNetwork is the LatticeNetwork name in RemoteNamespace.
                type: string
            required:
            - localNamespace
            - localNetwork
            - remoteCluster
            - remoteNamespace
            - remoteNetwork
            type: object
          status:
            description: LatticeClusterPeeringStatus reports the observed state of
              a cross-cluster peering.
            properties:
              conditions:
                description: Conditions contains fine-grained status conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              localCIDR:
                description: LocalCIDR is the ActiveCIDR of the local network, populated
                  once Ready.
                type: string
              phase:
                description: Phase is Pending | Ready | Error.
                type: string
              remoteCIDR:
                description: RemoteCIDR is the ActiveCIDR of the remote network, populated
                  once Ready.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: latticeclusters.alattice.io
spec:
  group: alattice.io
  names:
    kind: LatticeCluster
    listKind: LatticeClusterList
    plural: latticeclusters
    shortNames:
    - wfcluster
    singular: latticecluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .spec.managementEndpoint
      name: ENDPOINT
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          LatticeCluster registers a remote Lattice deployment so that
          LatticeClusterPeering resources can reference it.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LatticeClusterSpec describes a remote Lattice deployment that this cluster
              can establish cross-cluster peerings with.
            properties:
              credentialRef:
                description: |-
                  CredentialRef is the name of a Secret in the controller namespace that
                  holds the authentication token for the remote management API.
                  The Secret must have key "token".
                type: string
              managementEndpoint:
                description: |-
                  ManagementEndpoint is the HTTPS base URL of the remote cluster's
                  Lattice management API (e.g. "https://lattice.prod-eu.example.com").
                type: string
            required:
            - credentialRef
            - managementEndpoint
            type: object
          status:
            description: LatticeClusterStatus reports the observed connection state
              of a remote cluster.
            properties:
              conditions:
                description: Conditions contains fine-grained status conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastProbeTime:
                description: LastProbeTime is when the controller last successfully
                  contacted the remote cluster.
                format: date-time
                type: string
              phase:
                description: 'Phase is the current connection state: Connected | Disconnected
                  | Unknown.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: latticeendpoints.alattice.io
spec:
  group: alattice.io
  names:
    kind: LatticeEndpoint
    listKind: LatticeEndpointList
    plural: latticeendpoints
    shortNames:
    - wfe
    singular: latticeendpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The current address of the peer
      jsonPath: .status.address
      name: ADDRESS
      type: string
    - description: the endpoint's peer reference
      jsonPath: .spec.peerref
      name: PEER-REF
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              address:
                type: string
              networkRef:
                type: string
              peerRef:
                type: string
            required:
            - address
            - networkRef
            - peerRef
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: latticeenrollmenttokens.alattice.io
spec:
  group: alattice.io
  names:
    kind: LatticeEnrollmentToken
    listKind: LatticeEnrollmentTokenList
    plural: latticeenrollmenttokens
    shortNames:
    - wftoken
    singular: latticeenrollmenttoken
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: invite to namespace
      jsonPath: .spec.namespace
      name: NAMESPACE
      type: string
    - description: limit to invite
      jsonPath: .spec.usagelimit
      name: USAGELIMIT
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              boundPeers:
                items:
                  type: string
                type: array
              expiry:
                format: date-time
                type: string
              namespace:
                type: string
              token:
                type: string
              usageLimit:
                type: integer
            required:
            - expiry
            - namespace
            - token
            - usageLimit
            type: object
          status:
            properties:
              boundPeers:
                items:
                  type: string
                type: array
              isExpired:
                type: boolean
              phase:
                type: string
              token:
                type: string
              usedCount:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: latticeglobalippools.alattice.io
spec:
  group: alattice.io
  names:
    kind: LatticeGlobalIPPool
    listKind: LatticeGlobalIPPoolList
    plural: latticeglobalippools
    shortNames:
    - wfpool
    singular: latticeglobalippool
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.totalSubnets
      name: TOTAL
      type: integer
    - jsonPath: .status.allocatedSubnets
      name: ALLOCATED
      type: integer
    - jsonPath: .status.availableSubnets
      name: AVAILABLE
      type: integer
    - jsonPath: .status.usagePercentage
      name: USAGE
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LatticeGlobalIPPoolSpec define global ip pool
            properties:
              cidr:
                type: string
              subnetMask:
                type: integer
            required:
            - cidr
            - subnetMask
            type: object
          status:
            description: LatticeGlobalIPPoolStatus =
            properties:
              allocatedSubnets:
                description: AllocatedSubnets
                type: integer
              availableSubnets:
                description: AvailableSubnets
                type: integer
              conditions:
                description: Conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              totalSubnets:
                description: TotalSubnets total subnets
                type: integer
              usagePercentage:
                description: UsagePercentage
                type: string
            required:
            - allocatedSubnets
            - availableSubnets
            - totalSubnets
            - usagePercentage
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: latticeinvitations.alattice.io
spec:
  group: alattice.io
  names:
    kind: LatticeInvitation
    listKind: LatticeInvitationList
    plural: latticeinvitations
    shortNames:
    - wfinv
    singular: latticeinvitation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Joined Network
      jsonPath: .spec.network
      name: NETWORK
      type: string
    - description: Joined namespace
      jsonPath: .spec.namespace
      name: NAMESPACE
      type: string
    - description: Added labels when joined
      jsonPath: .spec.peerLabels
      name: LABELS
      type: string
    - description: Expires time
      jsonPath: .spec.expiresAt
      name: ExpiresAt
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LatticeInvitation is the Schema for the networks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              expiresAt:
                format: date-time
                type: string
              network:
                description: Invite a peer join to a network
                type: string
              peerLabels:
                additionalProperties:
                  type: string
                description: 'Preset labels for new peers (e.g., role: developer)'
                type: object
              targetNamespace:
                description: join to special namespace
                type: string
            required:
            - expiresAt
            - network
            - targetNamespace
            type: object
          status:
            properties:
              expiresAt:
                format: date-time
                type: string
              token:
                description: Token generated for the client
                type: string
              usedCount:
                description: Number of times used
                type: integer
              usedPeers:
                items:
                  type: string
                type: array
            required:
            - expiresAt
            - token
            - usedCount
            - usedPeers
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: latticenetworkpeerings.alattice.io
spec:
  group: alattice.io
  names:
    kind: LatticeNetworkPeering
    listKind: LatticeNetworkPeeringList
    plural: latticenetworkpeerings
    shortNames:
    - wfpeering
    singular: latticenetworkpeering
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .status.cidrA
      name: CIDR-A
      type: string
    - jsonPath: .status.cidrB
      name: CIDR-B
      type: string
    - jsonPath: .spec.peeringMode
      name: MODE
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          LatticeNetworkPeering connects two LatticeNetworks across different workspaces
          so their peers can communicate directly over WireGuard.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LatticeNetworkPeeringSpec declares a peering relationship between two networks
              that may reside in different namespaces (workspaces).
            properties:
              namespaceA:
                description: NamespaceA is the Kubernetes namespace of the first workspace.
                type: string
              namespaceB:
                description: NamespaceB is the Kubernetes namespace of the second
                  workspace.
                type: string
              networkA:
                description: NetworkA is the LatticeNetwork name in NamespaceA.
                type: string
              networkB:
                description: NetworkB is the LatticeNetwork name in NamespaceB.
                type: string
              peeringMode:
                default: gateway
                description: |-
                  PeeringMode controls the traffic forwarding strategy.
                  Defaults to "gateway".
                enum:
                - gateway
                - mesh
                type: string
            required:
            - namespaceA
            - namespaceB
            - networkA
            - networkB
            type: object
          status:
            description: LatticeNetworkPeeringStatus reports the observed state of
              the peering.
            properties:
              cidrA:
                description: CIDRA is the ActiveCIDR of NetworkA, populated once the
                  peering is Ready.
                type: string
              cidrB:
                description: CIDRB is the ActiveCIDR of NetworkB, populated once the
                  peering is Ready.
                type: string
              conditions:
                description: Conditions contains fine-grained status conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: 'Phase summarises the peering lifecycle: Pending | Ready
                  | Error.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: latticenetworks.alattice.io
spec:
  group: alattice.io
  names:
    kind: LatticeNetwork
    listKind: LatticeNetworkList
    plural: latticenetworks
    shortNames:
    - wfnet
    - wfnetwork
    singular: latticenetwork
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The current phase of the network
      jsonPath: .status.phase
      name: PHASE
      type: string
    - description: The CIDR block of the network
      jsonPath: .spec.cidr
      name: CIDR
      type: string
    - description: Number of nodes in the network
      jsonPath: .status.addedNodes
      name: NODES
      type: integer
    - description: Number of available IP addresses
      jsonPath: .status.availableIPs
      name: IPS-AVAIL
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LatticeNetwork is the Schema for the networks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LatticeNetworkSpec defines the desired state of LatticeNetwork.
            properties:
              cidr:
                type: string
              dns:
                properties:
                  enabled:
                    type: boolean
                  servers:
                    items:
                      type: string
                    type: array
                required:
                - enabled
                type: object
              mtu:
                type: integer
              name:
                description: name of network
                type: string
              networkId:
                type: string
              owner:
                type: string
              peerSelector:
                additionalProperties:
                  type: string
                type: object
              policies:
                items:
                  type: string
                type: array
            type: object
          status:
            description: LatticeNetworkStatus defines the observed state of LatticeNetwork.
            properties:
              activeCIDR:
                type: string
              allocatedCount:
                type: integer
              availableIPs:
                description: Available IP count
                type: integer
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: latticepeers.alattice.io
spec:
  group: alattice.io
  names:
    kind: LatticePeer
    listKind: LatticePeerList
    plural: latticepeers
    shortNames:
    - wfpeer
    singular: latticepeer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The current status of the node
      jsonPath: .status.status
      name: STATUS
      type: string
    - description: The current phase of the node
      jsonPath: .status.phase
      name: PHASE
      type: string
    - description: The IP address allocated to the node
      jsonPath: .status.allocatedAddress
      name: IP
      type: string
    - description: The network the node belongs to
      jsonPath: .spec.network
      name: NETWORK
      type: string
    - description: Number of active connections
      jsonPath: .status.connectionSummary.connected
      name: CONNECTED
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LatticePeer is the Schema for the nodes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LatticePeerSpec defines the desired state of LatticePeer.
            properties:
              allowedIPs:
                items:
                  type: string
                type: array
              appId:
                type: string
              dnsServers:
                items:
                  type: string
                type: array
              interfaceName:
                description: Interface for the node
                type: string
              lrpQuicUrl:
                description: |-
                  LrpQuicUrl is the QUIC address of the LRP relay server.
                  When set, nodes prefer QUIC over TCP for relay traffic.
                type: string
              relayUrl:
                description: |-
                  RelayUrl is the TCP address of the relay server assigned to this peer.
                  Populated by the relay settings controller when a relay is bound to the peer's workspace.
                type: string
              mtu:
                type: integer
              network:
                type: string
              networkPolicies:
                items:
                  type: string
                type: array
              peerId:
                type: string
              platform:
                description: platform which node runs on
                type: string
              privateKey:
                type: string
              publicKey:
                type: string
            type: object
          status:
            description: LatticePeerStatus defines the observed state of LatticePeer.
            properties:
              activeKey:
                description: Active key
                type: string
              activeNetwork:
                description: Active networks, record the network the node joined
                type: string
              activeNetworkPolicies:
                items:
                  type: string
                type: array
              allocatedAddress:
                description: Allocated IP address, auto allocated by controller
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectionSummary:
                description: Connection summary
                properties:
                  connected:
                    type: integer
                  disconnected:
                    type: integer
                  total:
                    type: integer
                required:
                - connected
                - disconnected
                - total
                type: object
              currentHash:
                description: message hash store here
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                description: client applied version
                format: int64
                type: integer
              phase:
                type: string
              status:
                description: LatticePeer status
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: latticepolicies.alattice.io
spec:
  group: alattice.io
  names:
    kind: LatticePolicy
    listKind: LatticePolicyList
    plural: latticepolicies
    shortNames:
    - wfpolicy
    singular: latticepolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The type of the network policy (ingress or egress)
      jsonPath: .spec.policyType
      name: TYPE
      type: string
    - description: The selector to identify nodes this policy applies to
      jsonPath: .spec.nodeSelector
      name: NODE-SELECTOR
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - description: Number of nodes targeted by this policy
      jsonPath: .status.targetNodes
      name: TARGETS
      type: integer
    - description: Number of rules defined in this policy
      jsonPath: .status.ruleCount
      name: RULES
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LatticePolicy is the Schema for the networkpolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LatticePolicySpec defines the desired state of LatticePolicy.
              which used to control the lattice's traffic flow.
            properties:
              action:
                description: default DENY
                type: string
              egress:
                items:
                  properties:
                    ports:
                      items:
                        properties:
                          port:
                            format: int32
                            type: integer
                          protocol:
                            type: string
                        type: object
                      type: array
                    to:
                      items:
                        properties:
                          ipBlock:
                            properties:
                              cidr:
                                type: string
                            type: object
                          peerSelector:
                            description: |-
                              A label selector is a label query over a set of resources. The result of matchLabels and
                              matchExpressions are ANDed. An empty label selector matches all objects. A null
                              label selector matches no objects.
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      type: array
                  type: object
                type: array
              expiresAt:
                description: |-
                  ExpiresAt is the optional time after which this policy will be automatically deleted.
                  Nil means the policy never expires.
                format: date-time
                type: string
              ingress:
                items:
                  description: IngressRule and EgressRule are used to control the
                    lattice's traffic flow.
                  properties:
                    from:
                      items:
                        properties:
                          ipBlock:
                            properties:
                              cidr:
                                type: string
                            type: object
                          peerSelector:
                            description: |-
                              A label selector is a label query over a set of resources. The result of matchLabels and
                              matchExpressions are ANDed. An empty label selector matches all objects. A null
                              label selector matches no objects.
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      type: array
                    ports:
                      items:
                        properties:
                          port:
                            format: int32
                            type: integer
                          protocol:
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
              network:
                type: string
              peerSelector:
                description: PeerSelector is a label query over node that should be
                  applied to the lattice policy.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - network
            type: object
          status:
            description: NetworkPolicyStatus defines the observed state of LatticePolicy.
            properties:
              ruleCount:
                description: Rule count (Ingress + Egress)
                type: integer
              targetNodes:
                description: Number of nodes currently matched by the policy
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: latticerelayservers.alattice.io
spec:
  group: alattice.io
  names:
    kind: LatticeRelayServer
    listKind: LatticeRelayServerList
    plural: latticerelayservers
    shortNames:
    - wfrelay
    singular: latticerelayserver
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DISPLAY
      type: string
    - jsonPath: .status.health
      name: HEALTH
      type: string
    - jsonPath: .status.connectedPeers
      name: PEERS
      type: integer
    - jsonPath: .spec.tcpUrl
      name: TCP
      type: string
    - jsonPath: .spec.enabled
      name: ENABLED
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          LatticeRelayServer is the Schema for managing LRP relay servers.
          It is cluster-scoped because relay infrastructure is shared across workspaces.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LatticeRelayServerSpec defines the desired state of an LRP
              relay server.
            properties:
              description:
                description: Description is an optional free-text note.
                type: string
              displayName:
                description: DisplayName is the human-readable label shown in the
                  management UI.
                type: string
              enabled:
                description: |-
                  Enabled controls whether this relay is pushed to nodes.
                  Disabled relays are not propagated; nodes retain their last-configured URLs
                  until a new enabled relay takes effect.
                type: boolean
              namespaces:
                description: |-
                  Namespaces is the list of Kubernetes namespaces (workspace namespaces)
                  whose LatticePeers should be configured to use this relay.
                  An empty list means all namespaces.
                items:
                  type: string
                type: array
              quicUrl:
                description: |-
                  QuicUrl is the host:port of the QUIC LRP relay endpoint.
                  Corresponds to --relay-quic-url on the client. Preferred over TCP when set.
                type: string
              tcpUrl:
                description: |-
                  TcpUrl is the host:port of the TCP LRP relay endpoint.
                  Corresponds to the node flag --lrp-url.
                type: string
            required:
            - displayName
            - enabled
            - tcpUrl
            type: object
          status:
            description: LatticeRelayServerStatus defines the observed state of a
              LatticeRelayServer.
            properties:
              conditions:
                description: Conditions holds standard Kubernetes condition records.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectedPeers:
                description: ConnectedPeers is the number of LatticePeers currently
                  configured to use this relay.
                type: integer
              health:
                description: Health is the result of the most recent connectivity
                  probe.
                type: string
              lastProbeTime:
                description: LastProbeTime is when the relay was last connectivity-tested.
                format: date-time
                type: string
              latencyMs:
                description: LatencyMs is the round-trip latency measured by the last
                  probe, in milliseconds.
                format: int64
                type: integer
              phase:
                description: Phase summarises the lifecycle state of the relay.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: latticesubnetallocations.alattice.io
spec:
  group: alattice.io
  names:
    kind: LatticeSubnetAllocation
    listKind: LatticeSubnetAllocationList
    plural: latticesubnetallocations
    shortNames:
    - wfsubnet
    singular: latticesubnetallocation
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          LatticeSubnetAllocation for store / search a network's cidr
          Its Name format is: subnet-<hex-ip> (e.g. subnet-0a0a0100)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              cidr:
                type: string
              networkName:
                type: string
            required:
            - cidr
            - networkName
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
