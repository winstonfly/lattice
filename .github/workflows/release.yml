name: Release

on:
  push:
    tags:
      - 'v*'
    branches:
      - master
  pull_request:
    types: [labeled, opened, synchronize, reopened]
    branches:
      - master
      - main

permissions:
  contents: write
  packages: write

env:
  REGISTRY: ghcr.io/alatticeio

jobs:
  release:
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - name: Compute build variables
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            VERSION="${{ github.ref_name }}"
            IS_RELEASE=true
            BUILD_LATEST=true
          elif [ "${{ github.ref }}" = "refs/heads/master" ]; then
            VERSION="$(echo ${{ github.sha }} | cut -c1-7)"
            IS_RELEASE=false
            BUILD_LATEST=true
          else
            VERSION="pr-${{ github.event.pull_request.number }}"
            IS_RELEASE=false
            BUILD_LATEST=false
          fi
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "IS_RELEASE=$IS_RELEASE" >> "$GITHUB_ENV"
          echo "BUILD_LATEST=$BUILD_LATEST" >> "$GITHUB_ENV"

      - name: Run GoReleaser (release)
        if: env.IS_RELEASE == 'true'
        uses: goreleaser/goreleaser-action@v6
        with:
          version: latest
          args: release --clean --skip=docker,homebrew
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}

      - name: Run GoReleaser (snapshot)
        if: env.IS_RELEASE != 'true' && github.event_name == 'pull_request'
        uses: goreleaser/goreleaser-action@v6
        with:
          version: latest
          args: release --snapshot --clean --skip=docker
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup pnpm
        run: npm install -g pnpm@10.32.1

      - name: Build UI
        run: make build-ui

      - name: Create buildx builder
        run: |
          docker buildx create --name lattice-builder --use
          docker buildx inspect --bootstrap

      - name: Login to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build community images
        run: |
          if [ "$BUILD_LATEST" = "true" ]; then
            OUTPUT="--push"
          else
            OUTPUT="--output type=cacheonly"
          fi
          for SERVICE in latticed lattice; do
            TAGS="--tag $REGISTRY/${SERVICE}:${VERSION}"
            [ "$BUILD_LATEST" = "true" ] && TAGS="$TAGS --tag $REGISTRY/${SERVICE}:latest"
            docker buildx build \
              --platform linux/amd64 \
              $OUTPUT \
              $TAGS \
              --build-arg "TARGETSERVICE=${SERVICE}" \
              --build-arg "VERSION=${VERSION}" \
              --build-arg "BUILD_TAGS=" \
              --label "org.opencontainers.image.title=${SERVICE}" \
              --label "org.opencontainers.image.version=${VERSION}" \
              --label "org.opencontainers.image.revision=${{ github.sha }}" \
              --label "org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}" \
              --cache-from "type=gha" \
              --cache-to "type=gha,mode=max" \
              -f Dockerfile .
          done

      - name: Install oras
        if: env.IS_RELEASE == 'true'
        uses: oras-project/setup-oras@v1

      - name: Build PRO images
        run: |
          if [ "$BUILD_LATEST" = "true" ]; then
            OUTPUT="--push"
          else
            OUTPUT="--output type=cacheonly"
          fi
          for SERVICE in latticed lattice; do
            TAGS="--tag $REGISTRY/${SERVICE}:${VERSION}-pro"
            [ "$BUILD_LATEST" = "true" ] && TAGS="$TAGS --tag $REGISTRY/${SERVICE}:latest-pro"
            docker buildx build \
              --platform linux/amd64 \
              $OUTPUT \
              $TAGS \
              --build-arg "TARGETSERVICE=${SERVICE}" \
              --build-arg "VERSION=${VERSION}" \
              --build-arg "BUILD_TAGS=pro" \
              --label "org.opencontainers.image.title=${SERVICE}" \
              --label "org.opencontainers.image.version=${VERSION}-pro" \
              --label "org.opencontainers.image.revision=${{ github.sha }}" \
              --label "org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}" \
              --cache-from "type=gha" \
              --cache-to "type=gha,mode=max" \
              -f Dockerfile .
          done

      - name: Package Helm chart (PR validation)
        if: github.event_name == 'pull_request'
        run: |
          helm package deploy/charts/lattice \
            --version "0.0.0-${VERSION}" \
            --app-version ${VERSION}
          echo "Chart packaged successfully: lattice-0.0.0-${VERSION}.tgz"

      - name: Publish Helm chart to GHCR
        if: env.IS_RELEASE == 'true'
        run: |
          CHART_VERSION=${VERSION#v}
          helm package deploy/charts/lattice \
            --version ${CHART_VERSION} \
            --app-version ${VERSION}
          helm push lattice-${CHART_VERSION}.tgz oci://ghcr.io/alatticeio/charts

      - name: Publish Artifact Hub repo metadata
        if: env.IS_RELEASE == 'true'
        run: |
          oras push ghcr.io/alatticeio/charts/artifacthub-repo:latest \
            --config /dev/null:application/vnd.cncf.artifacthub.config.v1+json \
            deploy/charts/lattice/artifacthub-repo.yml:application/vnd.cncf.artifacthub.repository-metadata.layer.v1.yaml
